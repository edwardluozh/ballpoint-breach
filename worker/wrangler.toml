name = "ballpoint-mp"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# Durable Objects
[[durable_objects.bindings]]
name = "ROOMS"
class_name = "GameRoom"

# Migrations for DO (using new_sqlite_classes for free plan)
[[migrations]]
tag = "v1"
new_sqlite_classes = ["GameRoom"]
